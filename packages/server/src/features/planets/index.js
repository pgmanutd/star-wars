import queries from './queries';
import resolvers from './resolvers';

import * as repositories from './planetsRepository';
import * as domainServices from './planetsDomainService';

export default {
  queries,
  resolvers,
  repositories,
  domainServices,
};
